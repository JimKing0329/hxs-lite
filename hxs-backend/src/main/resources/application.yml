server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/hsxf?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai
    username: root
    password: Wx050329
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  global-config:
    db-config:
      id-type: auto
  type-aliases-package: com.hxs.model.entity

# JWT 配置
hsxf:
  jwt:
    admin-secret-key: hsxf-admin
    admin-ttl: 259200000
    admin-token-name: Authorization
    user-secret-key: hsxf
    user-ttl: 2592000000
    user-token-name: token
  admin:
    sid: ${HSXF_ADMIN_SID:}
    password: ${HSXF_ADMIN_PWD:}

# 微信配置
wechat:
  app-id: ${HSXF_WECHAT_APP_ID:}
  app-secret: ${HSXF_WECHAT_APP_SECRET:}
